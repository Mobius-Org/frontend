import React from "react";
import styled from "styled-components";

function TopNav() {
  return <TopNavWrapper>index</TopNavWrapper>;
}

export { TopNav };

const TopNavWrapper = styled.div`
  width: calc(100vw - 320px);
  position: absolute;
  top: 0;
  right: 0;
`;
